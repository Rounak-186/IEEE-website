
export const metadata = {
    title: "Contact | IEEE JGEC Student Branch",
    description: "Get in touch with the IEEE JGEC Student Branch for queries, collaborations, membership details, or event participation. We’d love to connect with you!"
};

export default function TeamLayout({ children }: { children: React.ReactNode }) {
    return <>{children}</>;
}
